<nav class="navbar justify-content-end">
  <ul class="nav">
    <li class="nav-item">

      {{#unless user}}
        <button id="login" class="nav-link btn btn-primary" data-toggle="modal" data-target="#loginModal">Log in / Sign
        up</button>
      {{else}}
        <button id="profile" class="nav-link btn btn-success">My Profile</button>
        <button id="logout" class="nav-link btn btn-primary">Log out</button>
      {{/unless}}

    </li>
  </ul>
</nav>

<!-- Modal -->
<div class="modal fade" id="loginModal" tabindex="-1" role="dialog" aria-labelledby="loginModalTitle"
  aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="loginModalTitle">Log in / Sign up</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>

      <form id="login-form">

        <div class="modal-body">
          Email<br>
          <input id="email-input" type="email" style="width: 100%;" required><br>
          Password<br>
          <input id="password-input" type="password" style="width: 100%;" required><br>
        </div>
        <div class="modal-footer">
          <button type="submit" class="btn btn-primary" id="login">Log in</button>
          <button type="submit" class="btn btn-primary" id="signup">Sign up</button>
          <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
        </div>

      </form>

    </div>
  </div>
</div>

<div class="container">
  Search by drink name: <br>
  <input id="drink-name" type="text">
  <button id="name-search" class="btn btn-primary">Go</button><br>
  
  <br>

  Search by ingredient: <br>
  <input id="ing-name" type="text">
  <button id="ing-search" class="btn btn-primary">Go</button><br>

  Most popular cocktails: 
  <button id="most-pop" class="btn btn-primary">Go</button><br>

  Random cocktail: 
  <button id="random" class="btn btn-primary">Go</button><br>

  Search by category: 
  <select id="cat-dropdown">
    <option value="Ordinary Drink">Ordinary Drink</option>
    <option value="Cocktail">Cocktail</option>
    <option value="Milk / Float / Shake">Milk / Float / Shake</option>
    <option value="Other / Unknown">Other / Unknown</option>
    <option value="Cocoa">Cocoa</option>
    <option value="Shot">Shot</option>
    <option value="Coffee / Tea">Coffee / Tea</option>
    <option value="Homemade Liqueur">Homemade Liqueur</option>
    <option value="Punch / Party Drink">Punch / Party Drink</option>
    <option value="Beer">Beer</option>
    <option value="Soft Drink / Soda">Soft Drink / Soda</option>
  </select>
  <button id="cat-search" class="btn btn-primary">Go</button><br>

  <button id="test" class="btn btn-success">Test</button>

  <div id="results"></div>
</div>

{{!-- Custom/favorites container for user saved drinks cocktails--}}
{{!-- This will populate using the models and DB --}}
<div id="main py-5 bg-light">
  <div class="container">
    {{!-- when loading index page pushes msg here from htmlroutes--}}
    <h1>{{msg}}</h1>
    <div class="row">
      {{#each cocktails}}
        <div class="col-md-3" data-id="{{this.id}}">
          <div class="drink-card card mb-4 shadow-sm" data-pic="{{this.pic}}" data-id="{{this.id}}" data-name="{{this.name}}" data-category="{{this.category}}" data-glass="{{this.glass}}" data-instructions="{{this.instructions}}" data-ingredients="{{this.ing}}">
                  <img src="{{this.pic}}" class="card-img-top thumbnail" width="100%" height="300" preserveAspectRatio="xMidYMid slice" focusable="false"  aria-label="{{this.name}}"><title>{{this.name}}</title><rect width="100%" height="100%" fill="#55595c"></rect></img>
              <div class="card-body">
                <p class="drinkName card-text">{{this.name}}</p>
      {{!-- need to figure out how to capture liked drink --}}
                <i class="star far fa-star"></i>
              </div>
            </div>
          </div>
      {{/each}}
    </div>
  </div>
</div>
{{!-- End of custom/favorites container for user saved drinks cocktails --}}

{{!-- Main Page Display --}}

{{!-- End of main page container with popular cocktails --}}

{{!-- Cocktail Modal --}}
<div class="modal fade" id="cocktailModal" tabindex="-1" role="dialog" aria-labelledby="cocktailModalTitle"
  aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <img id="drinkImage" class="modal-image" src="" width="100%" height="350" preserveAspectRatio="xMidYMid slice" focusable="false">
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <h5 id="drinkTitle" class="card-title "><strong></strong></h5>
                <p id="drinkCategory"></p>
                <p id="drinkGlass"></p>
                <p id="drinkInstructions"></p>
                <p id="drinkIng"></p>
            <div class="modal-footer">
                <button type="button" class="btn btn-warning" data-dismiss="modal">Customize/Save</button>
                <a href="/">← Back To Home</a>
            </div>
        </div>
    </div>
</div>
      {{!-- End Cocktail Modal --}}
